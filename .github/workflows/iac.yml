name: IAC Deployment.
on:
  pull_request:
    types: [labeled]
    branches: [develop]

env:
  AZURE_RESOURCE_GROUP: cgc-azure-resume
  AZURE_LOCATION: '"Central US"'

jobs:
  setup-up-azure-resources:
    runs-on: ubuntu-latest
    
    if: contains(github.event.pull_request.labels.*.name, 'Create Infra')
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Azure login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
        
      - name: Create Azure Resource Group
        if: success()
        run: |
          pwd
          az group create --location ${{ env.AZURE_LOCATION }} \
            --name ${{ env.AZURE_RESOURCE_GROUP }} \
            --subscription ${{ secrets.AZURE_SUBSCRIPTION_ID }}
        
      - name: Create Storage Account in Resource Group
        uses: azure/arm-deploy@v1
        if: success()
        with:
          resourceGroupName: ${{ env.AZURE_RESOURCE_GROUP }}
          template: devops/storage-account-template.json
          parameters: devops/storage-account-parameters.json
          deploymentName: acg-resume-az-stge-account-deployment
        
      # - name: Create Cosmo DB
      #   uses: azure/arm-deploy@v1
      #   if: success()
      #   with:
      #     resourceGroupName: ${{ env.AZURE_RESOURCE_GROUP }}
      #     template: devops/cosmo-db-template.json
      #     parameters: devops/cosmo-db-parameters.json
      #     deploymentName: acg-resume-az-cosmos-db-deployment

      # - name: Create Azure Function App
      #   uses: azure/arm-deploy@v1
      #   if: success()
      #   with:
      #     resourceGroupName: ${{ env.AZURE_RESOURCE_GROUP }}
      #     template: devops/func-app-template.json
      #     parameters: devops/func-app-parameters.json
      #     deploymentName: acg-resume-az-function-app-deployment
            
  destroy-azure-resources:
    runs-on: ubuntu-latest
    
    if: contains(github.event.pull_request.labels.*.name, 'Destroy Infra')
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      
      - name: Destroy Azure Environment
        if: success()
        run: |
          az group delete --name ${{ env.AZURE_RESOURCE_GROUP }} \
            --subscription ${{ secrets.AZURE_SUBSCRIPTION_ID }} \
            --yes
    
